extends base
include mixins/video

block content 
  h1=user.name
  img(src="/" + user.avatarUrl, width=200, alt="user image")
  div=user.username 
  div=user.email 
  div=user.location

  each video in videos 
    +video(video)
      
  else 
    li  Sorry Nothing Found.